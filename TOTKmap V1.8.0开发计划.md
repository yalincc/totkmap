# TOTKmap V1.8.0 开发计划（实时导航 + 瓦片优化）

> 状态：**方案已确认，待开工**（2026-09-26）
> 本文档为 V1.8.0 唯一方案依据；开工后按里程碑推进，每阶段先给用户看效果再继续。

---

## 一、项目基本信息

| 项 | 内容 |
|---|---|
| 项目名称 | TOTKmap · 王国之泪·全能地图（本地离线互动地图，Leaflet） |
| 项目位置 | `E:\WorkSpace\TOTKmap` |
| 代码仓库 | GitHub `origin`：git@github.com:yalincc/totkmap.git；gitee：git@gitee.com:yalincc/totkmap.git |
| 当前版本 | V1.7.8 → 目标 **V1.8.0** |
| 相关文档 | `进度.md`（交接入口）、`开发知识点-TOTKmap.md`（踩坑沉淀）、根目录 `AGENTS.md`（协作规则） |
| 协作约定 | 方案先行确认后执行；每阶段看效果再继续；功能改动同步左下角版本号；README 只写功能简介 |

## 二、目标

1. **实时玩家定位（方案 B）**：本地服务读取 Ryujinx 内存中 TOTK 玩家坐标，地图显示红点 + 轨迹 + 金色引导线 + 距离。
2. **导航 + 两套收集队列**：导航到指定地点；收集 = 按「离玩家最近优先」自动续导，**探索队列与材料队列彻底分开**；跟随玩家 Z 自动切层（天空/地上/地底），**队列不跨层**；不保存路线。
3. **瓦片优化**：`tiles_obj` z3/z4 转 WebP q80（已完成，83.8MB → 62.9MB）。

## 三、已确认决策（用户拍板）

| # | 决策 |
|---|---|
| 1 | 导航走 **B：实时玩家定位**（先 Python 快速验证，再 Go 化） |
| 2 | 队列 A（探索）：池 = **勾选的单个或多个探索类别**（如 克洛格果实 + 卡邦达立牌 一起排），当前层内未完成目标，**哪个近去哪个** |
| 3 | 队列 B（材料）：池 = **勾选的单个或多个材料种类**（如 生命系列多个材料 一起排），当前层内未收集点位，离玩家最近优先 |
| 4 | 探索完成信号：V1 **手动「标记完成」为主**；**若已加载存档，以存档为准**（能逐点映射的类别按存档自动判定，映射核对见 M3） |
| 5 | 导航程序目录：快速验证放 **`TOTKmap/live-python/`**，Go 化放 **`TOTKmap/live-go/`**（明确区分，与 BOTWmap 的 live-go/live-float 同构思路） |
| 6 | 层判定：跟随玩家 Z 自动切层；队列只在本层内选目标，不跨层；该层清空即结束 |
| 7 | 排序规则：最近优先；**不保存路线**；导航面板（目标/距离/剩余/暂停/结束/到达提示） |
| 8 | 到达阈值：30 游戏单位（≈30m，可调，含义见第六节） |

## 四、架构

```
TOTKNavi 本地服务（live-python/ 快速验证 → live-go/ Go 单 exe）
 ├─ 定位：存档锚点 → 内存窗口扫描 → 旋转矩阵特征 → 短名单 → 走动确认
 │        → known_addrs 快路径 + watchdog 自愈（ZeldaTOTKmap 已实测，live-go 已有 Go 实现）
 ├─ GET  /pos      → {ok, mx, my, gz, layer, verified, source, age}
 ├─ POST /target   → {x, y, name, type} | {clear:true}
 └─ 端口 8766（CORS + PNA，与 BotwNavi 一致）
        ↑ fetch 轮询 600ms
TOTKmap 网页（js/app.js 新增 live 模块）
 ├─ 红点 + 轨迹 + 金色引导线 + 距离（画法参考 BOTWmap/app/js/live.js）
 ├─ 导航面板：目标名 / 距离 / 剩余 / 暂停 / 结束 / 到达提示
 ├─ 层自动切换：玩家高度 h = gz − 105；h≥950→天空(20)、h<0→地底(19)、否则地上(18)
 └─ 队列 A（探索）/ 队列 B（材料）
```

## 五、服务契约（Python/Go 共用，先定死 → 网页零改动）

| 端点 | 说明 |
|---|---|
| `GET /pos` | `{ok, mx, my, gz, layer, verified, source, age, progressGen}`；mx/my = 地图坐标（Leaflet latlng = 游戏坐标 Z/X），layer = 18/19/20；progressGen = `slot路径:mtime`（游戏内保存→变化→网页自动重拉进度） |
| `POST /target` | `{x, y, name, type}` 设置 / `{clear:true}` 清除（网页 → 服务，浮窗等客户端可共享） |
| `GET /rescan` | 手动重新定位（地址失效时） |
| `GET /progress` | `{ok, version, save, doneIds, mapped, counts, mtime}`；counts = 与 `js/save-parser.js collect()` 同口径的 20 类 `{done,total}`；doneIds = 逐点完成 id（鸟望台/龙之泪/魔犹伊）；缓存 2s |
| `GET /config` | 图层勾选同步（预留，浮窗等后续用） |

> V1.8.0 起 `/progress` 为服务端存档自动同步核心（对齐 BOTWmap live-go 机制：服务自动定位存档、网页加载一次即同步、游戏保存后自动刷新）；手动上传 `progress.sav` 保留为离线兜底。

## 六、到达阈值与距离（解释）

- **距离**：玩家实时坐标与目标点的欧氏距离 `hypot(ΔX, ΔZ)`，单位为 TOTK 游戏坐标单位（≈游戏内米，例：监视堡垒 CENTER=[-140.80, -298.54]、材料点 [-4431.53, 408.75, -3033.90] 均为同一坐标体系）。
- **到达阈值**：轮询中当 距离 ≤ 阈值（默认 30 单位）时判定「已到达」，导航面板提示"已到达 · 收集后自动前往下一个"，并**暂停自动切换**，等你完成（标记完成 / 存档判定）后自动选下一目标。
- 纯展示/提示阈值，不参与导航算法；BOTW 版用 15m，TOTK 建议 30 更宽松（防密集点位闪断），可随时调（做成面板内可调或常量）。

## 七、两套收集队列（关键区分）

| | **队列 A · 探索** | **队列 B · 材料** |
|---|---|---|
| 目标 | 神庙 / 鸟望塔 / 树根 / 克洛格 / 魔犹伊 / 贤者遗志 / 卡邦达 / 龙之泪…（探索标点） | 材料点位（110 种 / 44,279 点） |
| 池 | 当前层内、**已勾选探索类别**的未完成目标（`state.done` 为假） | 当前层内、**已勾选材料种类**的未收集点位（`matCollected` 为假，点级） |
| 完成信号 | 手动「标记完成」；**加载存档后以存档为准**（神庙/塔/树根等哈希逐点映射，M3 核对顺序；克洛格等无逐点 → 手动 + 存档计数） | 手动「收集/标记完成」（无存档记录，维持"重新勾选材料名=刷新重显"语义） |
| 前进规则 | 完成当前目标 → 自动选离玩家最近的下一未完成目标 | 同左 |
| 层 | 跟随玩家 Z 自动切层；只在本层选目标，不跨层；清空即结束 | 同左 |
| 互斥 | **探索收集过程中绝不跳到材料目标，反之亦然** | |

## 八、里程碑（每步先给用户看效果）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M0 | 瓦片优化（z3/z4 → WebP q80） | ✅ 已完成：83.8 → 62.9MB，画质无感 |
| M1 | `live-python/` 定位服务跑通：/pos 出红点 | 红点与游戏内位置一致；三层切换正确（标定通过）→ 截图确认 |
| M2 | 网页实时层 + 导航面板 | 红点/轨迹/引导线/距离/自动切层；面板交互（暂停/结束/到达提示） |
| M3 | 探索收集队列 A | 多类别勾选混合、最近优先、不跨层、存档逐点核对、手动兜底 |
| M4 | 材料收集队列 B | 多材料勾选混合、最近优先、不跨层、收集后自动续导 |
| M5 | Go 化 → `TOTKNavi.exe`（live-go/） | 契约不变，网页零改动；启动/自愈/known_addrs 验证 |
| M6 | 收尾 | 双端（PC/手机）验证；版本号 V1.8.0 三处同步；CHANGELOG；README 功能简介；按推送节奏 commit → 汇报 → 确认后 push + tag |

## 九、坐标与层标定（M1 第一件事）

- 内存格式：`mem = (X东西, Z高度+105, −Y南北)`（ZeldaTOTKmap 已实测，`ELEV_BIAS=105`）
- 地图坐标：`lng = X = mem[0]`；`lat = Z = −mem[2]`（符号按实测校准：游戏内走动验证红点方向）
- 层阈值：`h = gz − 105`；`h≥950 → 20 天空`、`h<0 → 19 地底`、`否则 → 18 地上`（对照 TOTKmap 数据层，实测校准）
- 验证锚点：监视堡垒（CENTER [-140.80, -298.54]）、15 座鸟望塔

## 十、风险

- 内存定位需在用户 Ryujinx / 游戏版本上实测调参（BOTW 踩坑：锁错槽 / 坐标漂移 / 自愈重启，live-go 有完整自愈方案可移植）
- 杀软对 ReadProcessMemory 的拦截（BotwNavi FAQ 已记录白名单处理）
- 存档逐点映射（哈希表顺序 ↔ 标记 id）需核对；无法映射的类别退回手动标记
- 手机端：红点/引导线渲染性能、导航面板布局适配

## 十一、文档规范（用户要求，长期遵守）

- 文件名 = 项目名 + 版本 + 用途，如 **`TOTKmap V1.8.0开发计划.md`**（不再用 `开发计划-材料追踪.md` 这类不明确命名）
- 文档首部必须含：项目名称 / 项目路径 / 代码仓库 / 当前版本 / 相关文档 等基本信息块


## 十二、M2 实测反馈落实（2026-09-26）

### 已修复
- **坐标映射**：实现误将 lat 写成 `+gy`，已按「九、坐标与层标定」修正为 `lat = -gy = mem[2]`。
  （TOTKmap lat 轴北负南正，与游戏地图显示一致；实测读数 0598-2120 0098 ↔ /pos (gx=599, gy=-2121, gz=99) 逐项吻合，修正后红点落点与游戏实际位置一致）
- **导航面板**：默认位置移到左侧栏之外（left:366px，不挡左侧栏）；按住面板头部可拖动（拖动后转 left/top 定位，移动端保持底部通栏）。
- **导航按钮**：详情卡「导航」为切换按钮——设置目标后变「停止导航」，再次点击清除目标并复原；按钮初始文案随目标状态显示。

### 后续硬性要求（用户明确）
- **M3 探索队列：探索卡片系统与材料卡片系统完全独立**——两套卡片不共用、不互相抄样式/交互（用户原话：不要抄材料卡片）。
- **M4 收集队列**：材料卡「收集」按钮接入实时导航队列（当前 M2 不启用，按钮保留但提示后续版本）。
- 到达阈值 30 游戏单位 ≈ 30m（面板可调 1-500，仅提示值，不参与算法）。
## 十三、M3 探索收集 —— 第一版（已否决）与返工（已完成，2026-09-26）

### 第一版「探索收集队列」（用户否决，已删除）
第一版做成了「独立队列区块 UI」（类别多选 chips + 开始/结束 + 状态行 + 跳过），端到端实测通过（勾选鸟望台+神庙→剩余135→最近目标7m→到达→标记完成→续导259m→结束），但被用户明确否决：
> 你的探索收集列队和我要的不是一个东西……我点击鸟望塔、神庙这两个，地图上出现了这两类图标，然后我点其中一个图标，出现卡片，点击卡片的【自动】按钮，则会根据地图上的图标进行自动导航……列队逻辑、阈值什么的基本都不用。
否决后删除：index.html 队列区块、js/explore-queue.js、.queue-block/.eq-* 样式。保留复用：存档逐点映射 data/explore_save_map.js、js/save-parser.js 的 pointDone()、LIVENAV 的 pos()/arrived()、TOTK_APP 桥接。

### 返工实现（当前状态）
- **操作流**：勾选类别 → 地图显示标点 → 点标点出 BOTW 风格探索卡片（类别 chip / 名称 / 区域=nearestRegion / 塔域=最近鸟望台（15 塔，TOTK 无现成塔域分区）/ 坐标 X·Z / 试炼=神庙 desc「名称：XXX」，无试炼行则隐藏）→ 三个按钮：导航 / 自动导航（再点=停止）/ 标记完成
- **自动导航** js/explore-auto.js（window.EXPLORE_AUTO）：池=当前层 + 当前勾选类别 + 未完成（state.done 含存档注入）；最近优先；跟随玩家 Z 切层重建池（不跨层）；到达（LIVENAV.arrived）后——
  - 可逐点判定类别（鸟望台/龙之泪/魔犹伊）+ 服务在线 → 轮询 GET /progress（5s）：doneIds 含当前目标 → applyProgressDone 合并 → 自动续导下一个；否则继续等（等游戏存档保存后自动检测）
  - 不可判定类别 / 服务离线 → 等手动「标记完成」→ 续导
- **服务端** live-python/server.py 新增 GET /progress（Python 复刻 js/save-parser.js 解析：版本表 / HASH_TABLE_END=0x03c800 / 0xa3db7114 哨兵 / GUID 格式；读 data/explore_save_map.js；**优先 slot_00**（Ryujinx 保存时 6 槽 mtime 全同，mtime 判槽不可靠）；2s 缓存）。实测：slot_00 mapped=118、doneIds=[119 监视堡垒鸟望台, 135 泡泡拉高地鸟望台, 2976(一只已收集魔犹伊)] 与用户真实存档一致
- **实测（浏览器端到端）**：勾选神庙 → 点斯斯毕艾神庙卡片（区域 阿德亚村遗迹 / 塔域 泡泡拉高地鸟望台 / X 354 · Z 2043 / 试炼 劳鲁的祝福）→ 自动导航启动 → 标记完成 → 自动续导吉乌可乌每神庙；模拟「到达+存档检测」：/progress 判定完成 → 自动续导下一最近目标（fetch 拦截确定性测试通过）

## 十四、存档自动同步（新增需求，已完成，2026-09-26）

**需求**（用户："你看看 botwmap 加载存档是怎么弄的…只要加载一次，就基本可以同步游戏信息的"）：对齐 BOTWmap live-go——服务端自动定位存档（不用上传文件），网页加载一次即同步游戏进度，游戏内保存后 ~1-2s 自动刷新。

### 槽位调查结论（用户反馈"存档位置不对/塔数 2 不变"）
- **6 槽 mtime 全同**（17:08:52）：Ryujinx 保存把所有槽 mtime 写成同一时刻 → mtime 判槽不可靠，原 `_find_save` 会随机选中非当前槽。
- **各槽 md5 不同但完成度一致**（同一周目）：塔数均为 2/15 → 计数不受槽位影响。
- **slot_00 逐塔 dump**（15 塔 hash 全查）：仅 119 监视堡垒 / 135 泡泡拉高地 value=1，其余 13 座全 0 → **存档事实就是 2 塔**（用户到塔但未在塔底发光处互动激活，或刚保存时未到激活点），非读错槽；网页不自动刷新是旧版行为，本次一并改。

### 实现
- **服务端**（live-python/server.py）：
  - `_find_save`：**slot_00 优先**（用户实际槽），不存在则回退 mtime 最新。
  - `_murmur3_32`：与 js `murmurHash3.x86.hash32` 完全一致（'hello'=0x248bfa47、'Clear'=0x62965740、'Open'=0x1818ec02 验证通过）。
  - `_parse_completism`：读 `data/totk_save_hashes.js` 的 CompletismHashes（踩坑：① 文件尾是 `}` 无分号；② 数组注释含 `[x,y,z]` 会截断非贪婪正则 → 按"换行+缩进 `]`"闭合匹配）。
  - `_progress_counts`：20 类与 `js/save-parser.js collect()` 同口径（神庙/鸟望台/树根/龙之泪/克洛格/双倍/魔犹伊/残旧地图/贤者遗志/设计图石板/卡邦达/6 类 BOSS/地洞/洞穴/井），随 /progress 返回。
  - `/pos` 增加 `progressGen` = `slot路径:mtime`（游戏保存 → mtime 变化 → 代次变化）。
  - 缓存 5s → **2s**。
- **前端**：
  - `js/live.js` poll()：检测 `/pos.progressGen` 变化（**服务可达即检测，不依赖定位成功**；3s 节流）→ 调 `TOTK_APP.syncProgressFromServer()`。
  - `js/app.js`：新增 `syncProgressFromServer()`——`applyProgressDone(doneIds)`（逐点完成注入）+ `state.save = counts` → `renderProgress()`（探索度自动显示）+ `buildCatalogPanel()` + `updateCount()`；按钮显示「存档自动同步 ✓（slot_XX）」；`state.saveVersion` 标注「· 服务端自动同步」。
  - 手动上传 `progress.sav`（saveSyncBtn）保留为离线兜底。

### 实测（浏览器端到端）
- 页面加载**无需上传**自动显示：神庙 8/152、鸟望台 2/15、树根 0/120、克洛格 4/800、双倍克洛格 2/100、魔犹伊 4/147、卡邦达立牌 1/81（与 slot_00 真实存档一致）。
- touch 存档 progress.sav mtime → 网页自动重新拉取 `/progress`（network 观测到新请求）✓。
- 逐点注入生效：doneIds 含 119/135 → 对应塔标点完成态。
