# 地名规范（TOTKMAP · 王国之泪互动地图）

> 本文档是地图地名系统的完整规范：**数据来源、坐标映射、分层显隐、文字式样、渲染实现**。
> 改地图（本项目的 TOTKmap，或其它要复刻同样地名的地图）时，按本文档取值，避免踩已修过的坑。
> 坐标换算等技术细节见 [README 坐标映射章](README.md)；本文重点在**地名文字式样**与**行为规范**。

---

## 1. 文字式样（核心）

> 目标观感：**贴合游戏内地图的米黄字 + 深色描边**，金褐实色（非半透明），暗色瓦片上清晰可读。

### 1.1 颜色

| 项 | 值 | 说明 |
|---|---|---|
| 文字颜色 | `#bfae68` | 金褐实色，**无透明度**（用户自选；objmap 为半透明 `#ccbb72`，本项目弃用） |
| 描边颜色 | `#3f3010` | 暖褐，四向 1.5px 实心描边 |
| 投影 | `rgba(0,0,0,.5)` / `rgba(30,20,0,.75)` / `rgba(30,20,0,.55)` | 下方柔和投影 + 暖褐泛光，增强暗背景可读性 |

### 1.2 字体

| 项 | 值 |
|---|---|
| 字体族 | `"PingFang SC", "Microsoft YaHei", "SimSun", sans-serif` |
| 字重 | `600`（半粗，贴合游戏标题字感） |
| 字距 | `letter-spacing: .5px` |
| 行高 | `line-height: 1.15` |
| 换行 | `white-space: nowrap`（单行不换行） |

### 1.3 字号（分级 + 随缩放动态放大）

地名数据带 `size` 基准值，随 ShowLevel 分级：

| ShowLevel | size 基准 |
|---|---|
| Farthest（最远，区域级） | 17 |
| Near / Far（较近） | 15 |
| Nearest / 空（细节级） | 13 |

**运行时时字号** = `max(11, size * (0.5 + zoom * 0.13))` px（随缩放逐级放大，低缩放不小于 11px）：

| zoom | size 17 | size 15 | size 13 |
|---|---|---|---|
| 3 | 15.1 | 13.4 | 11.6 |
| 4 | 17.3 | 15.3 | 13.3 |
| 5 | 19.6 | 17.3 | 14.9 |
| 6 | 21.8 | 19.2 | 16.6 |
| 7 | 24.0 | 21.2 | 18.3 |

### 1.4 完整 CSS（直接可抄）

```css
.area-label {                    /* divIcon 根节点 */
  background: none;
  color: #bfae68;
  border: none;
  border-radius: 0;
  padding: 0;
  font-weight: 600;
  font-family: "PingFang SC", "Microsoft YaHei", "SimSun", sans-serif;
  text-shadow:
    -1.5px -1.5px 0 #3f3010, 1.5px -1.5px 0 #3f3010,   /* 四向实心描边 */
    -1.5px 1.5px 0 #3f3010, 1.5px 1.5px 0 #3f3010,
    0 2px 4px rgba(0,0,0,.5),                          /* 下方投影 */
    0 0 3px rgba(30,20,0,.75), 0 0 6px rgba(30,20,0,.55); /* 暖褐泛光 */
  white-space: nowrap;
  pointer-events: none;          /* 不拦截地图点击 */
  letter-spacing: .5px;
}
.area-label-wrap {               /* 居中定位层 */
  display: inline-block;
  transform: translate(-50%, -50%);   /* 文字中心 = 坐标点投影 */
}
.area-label-inner {              /* 实际文字层（防重叠只动这一层） */
  display: inline-block;
  white-space: nowrap;
  line-height: 1.15;
}
```

---

## 2. 渲染锚点（勿改回默认）

- 地名用 Leaflet `L.divIcon` 创建，**必须 `iconSize: [0, 0]`**——锚点即坐标点本身。
- HTML 结构：`<div class="area-label-wrap"><div class="area-label-inner" data-prio="N">地名</div></div>`
  - `wrap` 负责 `translate(-50%,-50%)` **居中**（对齐 objmap 的 `bindTooltip(direction:'center')`，文字中心=坐标点）；
  - `inner` 负责**防重叠偏移/隐藏**（不破坏居中）；
- 实测文字中心与坐标点投影差 `(0,0)`。**不要改回 divIcon 默认 `[12,12]` 锚点**（会导致文字整体偏右下，如当年卡洛克桥偏右的问题）。

---

## 3. 行为规范

### 3.1 分层显隐（ShowLevel → visible）

objmap 每个坐标点带 ShowLevel（逗号分隔），映射到缩放可见级别（**勿改**）：

| ShowLevel | 可见 zoom |
|---|---|
| Farthest | 3, 4 |
| Far / Near | 5 |
| Nearest / 空 | 6, 7 |
| Place 类（村庄等） | 3-7 恒显 |

- **每个坐标点独立算自己的 visible**，禁止把同名多点的 ShowLevel 合并（否则同缩放出现 2 个同名字，如当年的"中央广场遗迹×2"）。
- ShowLevel 判断用**逗号 token 精确匹配**（`"Near"`、`"Nearest"` 按等值判断），**禁止 `"Near" in sl` 子串判断**——`"Nearest"` 含子串 `"Near"`，会把细节级误判成近景级同时显示。
- 优先级 prio（防重叠排序用）：MapRegion=1，Farthest=1，Near/Far=2，Nearest=3，空=4；**数字越小越靠前保留**。

### 3.2 三层判层（天空 / 地表 / 地底）

以 objmap `Translate.Y` 为权威：

| Y | 层 |
|---|---|
| `Y >= 950` | 天空（layer 20） |
| `Y < 0` | 地底（layer 19） |
| 其他 | 地表（layer 18） |

- **禁止用 CSV 的 field 字段判天空**（field=MainField 的地名可能是天空诸岛）。
- 注意 `MapRegion_Firone`（地表）与 `MapRegion_Firone_Sky`（天空）等成对 id **坐标完全相同**，按名字/层区分，勿混。

### 3.3 防重叠

- zoomend/moveend 后 80ms / 450ms / 1200ms 三阶段重算（等 Leaflet zoom 动画稳定后取 rect 才准，动画中取值会误判）。
- 按 prio 升序排列，低优先级尝试 25 个偏移位（上下左右 16/32/48/64/80px 网格 + 对角），全部被占则隐藏。

---

## 4. 数据规范

### 4.1 地名范围

- **只取 objmap static.json `markers.Location` 分组的坐标点**（890 点 / 711 唯一名）+ 少量区域补充（城市/竞技场/天空诸岛/泉水等官方中文 CSV）。
- **剔除有地图图标的类型**（不做地名文字，左侧栏单独控制）：洞穴（CaveEntranceNormal）、神庙（Shrine）、深穴（CaveEntranceSpecial）、井（CaveEntranceWell）、鸟望台（Tower）、驿站（Stable），共剔除约 397 条；另剔除商店/魔人像等非地名。
- objmap Location 分组内的"东坑道/西坑道/马尔鼓坑道/驿站村遗迹/神庙岛"等是**官方地名**，保留。
- 现状：areas 共 **913 条**（地表 706 / 地底 154 / 天空 53，与 objmap Y 核对 0 不一致）。

### 4.2 中文名来源（勿机翻）

1. 主源：`totk_msbt_zh_full.csv`（romfs 官方简体中文，label→zh）；
2. 兜底：`totk_zh_names.csv`（name_en→zh）；
3. 英文名：objmap `text/StaticMsg/LocationMarker.json`。

### 4.3 坐标基准

- 游戏坐标：X∈[-6000,6000]（东向正）、Z∈[-5000,5000]（北为负）。
- 地图 latlng = (Z, X)，即 areas 字段 `x←Z`、`y←X`。
- 地名坐标**全部来自 objmap static.json 的 Translate(X/Y/Z)**（不是 gamersky、不是 romfs）。历次发现 CSV 坐标与 objmap 有偏差（地表 20-110、天空/地底 300-740 单位），已统一替换为 objmap 坐标。

### 4.4 同名多坐标

- 河流/护城河/湖泊/山脉/废矿等分段地名，objmap 同 MessageID 有多个坐标点（希麦加米河 8 点、海拉鲁城堡护城河 8 点、沃尔纳山 5 点等），**全部保留逐点展开**。
- 去重键 = `(layer, name, 坐标)`，仅同坐标同名才去重。
- 同 id 多点 ShowLevel 可能不同（如中央广场遗迹：1 点 Near→z5、1 点 Nearest→z6-7），由 3.1 的独立分层保证同一缩放只显示 1 个。

---

## 5. 修改指引（改其它地图时）

- **只想调颜色/描边**：改第 1.4 节 `.area-label` 的 `color` 与 `text-shadow` 两行即可。
- **想调字号大小**：改各 ShowLevel 的 size 基准，或动态公式 `0.5 + zoom * 0.13` 的系数。
- **想调字体**：改 `font-family` 与 `font-weight`。
- **想改地名数据**：按第 4 节重跑生成脚本（规则都在 README 坐标映射章 + 本节，勿重新推导换算）。
- 改完记得同步：版本号三处（js/app.js `VERSION`、index.html `.foot-version` 与 `.version`）。
