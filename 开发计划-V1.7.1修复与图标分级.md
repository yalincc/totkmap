# 开发计划：V1.7.1 修复轮 + 材料图标分级

> 2026-09-26 · TOTKmap · 依据 AI-CORE WORKFLOW S2.5 落盘
> 前情：V1.7.0 已本地提交 `12d0722c`（未推送），CHANGELOG 遗留 5 个已知问题，本次统一解决并加图标分级。

## 版本目标
V1.7.1 —— 修复 3 个交互 bug（材料聚合不刷新 / 视口外不渲染 / 探索度无法收起）+ 地底矿物正名 + 图标高清核对 + 材料图标随缩放分级。

## 已确认决策（用户拍板）
1. 材料搜索框：**补全方案 A**（JS 逻辑与 CSS `.mat-search-box` 已就绪，仅 HTML 缺失导致 `app.js:1218` 崩溃）
2. 琥珀改名：**按实际名，用户不参与**——查证结果：`Obj_MineralGrain_A_03` 即 Zonaite Deposit（左纳尼乌姆矿，ZeldaWiki 确认几乎只在地底，与数据地表228/地底4386吻合），官方简中名「左纳尼乌姆」，命名与同系列一致为**「散落的左纳尼乌姆」**；图标同步换 `Item_Ore_L.png`（左纳尼乌姆矿石图）
3. 高清图标核对：**做**（75 个已高清跳过；11 个矿物对象映射高清矿石图；24 个昆虫高清包无对应，保留旧图，后续可 romfs 提取）

## 根因（已确诊，浏览器实测复现）
- `index.html` 材料面板缺 `matSearchInput` 搜索框 → `app.js:1218` 对 null 绑定抛 TypeError → **IIFE 中断**，后续绑定全部失效：
  - `map.on('moveend zoomend', renderMaterials)`（1264）未绑定 → 材料层不随缩放/平移重绘 → 「最大仍聚合」「视口外不渲染」
  - `progressHead` 折叠绑定（1324）未绑定 → 「探索度无法收起」
- 修复件已就位：supercluster `maxZoom=6`（Node 实测 z=7 全叶子）、bbox padding=800 —— 修掉崩溃即一并解决

## 数据源 / 路径
- 材料数据管道：`tools/build_materials_data.py`（ROM=`G:\YUZU\Switch Games\TOTKroms`，可重跑，幂等）
- 高清图标包：`E:\WorkSpace\BOTWroms\TOTKicons\`（233 张 256×256，ore/ 目录 13 张矿石图）
- 当前资产：`assets/materials/`（110 png + 35 冗余 webp）

## 技术方案与实施步骤
1. **补搜索框**：`index.html` 材料面板 toolbar 下加 `.mat-search-box`（`#matSearchInput` + `#matSearchResult`），CSS 补 input 样式
2. **数据正名**：`build_materials_data.py` `MANUAL_ZH['Obj_MineralGrain_A_03']` → `散落的左纳尼乌姆`，重跑管道重建 `data/materials.js`（保持幂等）
3. **图标高清化**：脚本将 11 个矿物对象 entry 复制 ore/ 高清图为 `assets/materials/<entry>.png`；删 35 个冗余 webp
4. **图标分级缩放**（参考地名字号调整思路）：
   - 单点 `mat-leaf`：`leafSize(zoom) = {3:20, 4:22, 5:26, 6:30, 7:34}` px（zoom 越大图标越大，同地名逻辑）
   - 聚合 `mat-cluster`：`base=leafSize(zoom)`，按 `point_count` 分档加成 `{<20:0, 20-99:+6, ≥100:+12}`，iconSize/iconAnchor 同步
5. **版本号**：`app.js VERSION`、`index.html` 两处 footer → `TOTKMAP V1.7.1`；缓存参数 `?v=181`；README 底部同步
6. **验证**：本地服务实测——3 bug 消失 + 搜索可用 + 图标分级生效 + 无 console 报错（PC + 手机视口）
7. **交付流程（R11）**：本地 commit → 用户确认 → push 双平台 + tag v1.7.1 → 归档（进度.md + PROJECT-INDEX 回填 V1.7.1）

## 验收标准
- [ ] 材料 Tab 搜索框输入即过滤（JS 已就绪）
- [ ] 勾选宁静公主 → 放大到 z7 → 119 点全部展开（0 聚合）
- [ ] 平移地图 → 视口外新材料点渲染（padding=800 生效）
- [ ] 探索度可收起/展开
- [ ] 图标随 zoom 尺寸递增；聚合徽标按数量分档
- [ ] 地底矿物条目名「散落的左纳尼乌姆」+ 图标为左纳尼乌姆矿石
- [ ] 无 console 报错；PC + 手机视口通过
- [ ] 版本号三处 V1.7.1 + `?v=181` + README 一致

## 待拍板项
- 图标分级的具体尺寸档位（先按上表实施，用户看效果后微调）
- 24 种昆虫高清图标：本次保留旧图，后续是否需要从 romfs bntx 提取（另立小任务）

## 不做（取舍）
- 不改聚合策略（maxZoom=6 已验证最大缩放全叶子）
- 不新增其他材料功能（收藏/联动均已有）
- 不处理 CHANGELOG 之外的其他项目问题
