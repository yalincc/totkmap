# 开发计划·材料追踪图层（TOTK 王国之泪）

> 状态：**待用户确认** ｜ 方案日期：2026-09-25
> 目标：在现有 TOTKmap（V1.6.0，Leaflet 三图层）上新增「材料」独立 Tab，复刻 BOTWmap 材料页体验——
> 左侧手风琴分类 + 搜索 + supercluster 聚合 + 每材料独立图标，标注全游戏可采集材料刷点。

---

## 一、已实测的数据结论（2026-09-25 探针验证）

直接解 `G:\YUZU\Switch Games\TOTKroms\Banc\{MainField,MinusField}\*_{Static,Dynamic}.bcett.byml.zs`
（zstd + bcett.byml.zsdic 字典 + 小端 BYML v7，Python `byml` 库解析通过）：

| 字段 | Static | Dynamic | 合计 |
|---|---|---|---|
| MainField（地表+天空岛） | 4,631 | 10,669 | **15,300** |
| MinusField（地底） | 1,049 | 4,925 | **5,974** |
| **合计** | 5,680 | 15,594 | **21,274 刷点 / 93 种材料 actor** |

> SpiritualField 目录在 v1.0.0 RomFS 导出里不存在；天空岛材料混在 MainField 高 Y 值点里（Y≥950 判天空层，与现有 markers 分层规则一致）。

按大类汇总（已过滤 `OwnedByNpc/Temporary/GoronDisplay/Kakariko` 任务变体）：

| 大类 | 刷点数 | 典型 actor 前缀 |
|---|---|---|
| 矿岩（矿脉） | 5,797 | `Obj_Mineral_A/B/C/D_01/02`（D 最多 5423） |
| 水果/树果 | 5,173 | `Item_Fruit_A` ~ `Item_Fruit_P` |
| 蘑菇 | 4,460 | `Item_Mushroom_A` ~ `Item_Mushroom_P` |
| 植物/草 | 3,184 | `Item_Plant_A/B/E/F/G/H/I/J/O/R/S` |
| 昆虫 | 1,568 | `Animal_Insect_A/AI/C/E/G/H/N/Q/S/T/X/Z/AG/EP...` |
| 鱼 | 900 | `Animal_Fish_A/E/F/G/H/I/J/L` |
| 矿石（掉落物） | 192 | `Item_Ore_A/B/C/E/H/I/L/M` |

坐标样本（Translate=[x, y, z]，y=海拔）：
```
Item_Fruit_B  [-4431.53, 408.75, -3033.90]   ← 地表
Item_Mushroom_D [-4499.14, 183.55, -2055.99] ← 地表
```
与 TOTKmap 现有坐标系（Leaflet latlng = (Z, X)，X∈[-6000,6000]，Z∈[-5000,5000]）**直接对齐**，无需仿射换算。

### 中文名与图标现状

| 资源 | 现状 |
|---|---|
| 官方简中物品名 | **尚未提取**。现有 `totk_msbt_zh_full.csv` 只含地名/神庙 1709 条；但工具链现成（`msbt_parse.py` + `CNzh.Product.100.sarc` 5.7MB 在本地），SARC 内 1510 个 MSBT，需对 `Item.msbt`/`CapturedActor.msbt` 重跑一遍即可 |
| 物品图标 | RomFS 无 `UI/StockItem/` 目录；图标打包在 `UI/LayoutArchive/Common.Product.110.*.blarc.zs` 内（timg/__Combined.bntx）。**备选快捷路径**：objmap-totk 网站本身有物品图标，可参考其 actor→icon 映射 |

---

## 二、能否沿用 BOTWmap 材料页框架？

**结论：交互框架可沿用，渲染层需适配 Leaflet。**

| 维度 | BOTWmap 现状 | TOTKmap 适配 |
|---|---|---|
| 地图引擎 | 自研 canvas（map.js 61KB） | **Leaflet**（已 vendor） |
| 聚合方案 | supercluster.min.js（已本地化，9KB） | 直接复用同一个 supercluster；用 `L.layerGroup` + canvas renderer 或 `L.divIcon` 渲染聚合点 |
| 侧栏 UI | 探索/材料双 Tab + 手风琴 6 类 + 每材料 checkbox + 搜索 | TOTKmap 现无 Tab，需在左侧 panel 顶部加「探索/材料」切换；材料 Tab 内嵌搜索 + 手风琴分类列表 |
| 数据结构 | `materials.js` = meta + materials[] + points(flat) + lod{} | 可沿用同构格式，新增 `layer` 字段（18地表/19地底/20天空）按 Y 判层 |
| 坐标系 | px = gameX*2+12000, py = gameZ*2+10000 | Leaflet latlng = (Z, X) 直接用游戏坐标，无需换算 |
| 三层支持 | BOTW 只有地表 | TOTKmap 已有 地上/地下/天空 三按钮；材料点按 Y 自动落到对应层，切层时只显该层材料 |
| 性能 | 13,524 点 supercluster 懒建实测流畅 | TOTK 21,274 点（地底多 6k），同样 supercluster 懒建，风险可控 |
| 进度同步 | BOTW 未做图鉴同步 | 本期同样不做；材料点纯展示+手动勾选「已收集」 |

---

## 三、技术方案

### P0 数据管道（一次性脚本，放 `tools/`）

1. **解 bcett 全量扫点**（探针已跑通）：
   - 遍历 `Banc/MainField` + `Banc/MinusField` 的 `*_Static/_Dynamic.bcett.byml.zs`
   - zstd(bcett 字典) → byml 解析 → 过滤材料 actor 前缀 → 提取 Translate [x,y,z]
   - 按 Y 判层：Y≥950→20 天空，Y≤-50→19 地底，其余→18 地表（与现有 markers 规则一致）
   - 坐标去重（同 (layer,x,z) 去重）
2. **中文名提取**：用 `msbt_parse.py` 解 `CNzh.Product.100.sarc` 内的 `Item.msbt` + `CapturedActor.msbt`，label = actor 名（如 `Item_Fruit_B_Name`）→ 中文。
3. **actor→材料映射表**：TOTK 的 `Item_Fruit_A..P` 是泛型树果 actor，需对照图鉴确定每个字母代表哪种果（苹果/香蕉/榴莲……）。映射表初版手工整理 + MSBT label 校验。
4. **生成 `data/materials.js`**：结构同 BOTW，每条 `{id, cat, cn, en, actor, layer, count}`，点位 flat 数组 `[matId, layer, x, z]`。

### P1 前端（`js/app.js` + `index.html` + `css/style.css`）

1. **侧栏加 Tab**：panel-top 下方加「探索 / 材料」两个按钮（参考 BOTW 截图）。
   - 探索 Tab：维持现状（catalogList + 探索度 + 存档）
   - 材料 Tab：材料搜索框 + 手风琴分类列表（植物/蘑菇/水果/昆虫/鱼/矿岩 6 类），每类默认折叠显示 `类名(N点) [全选]`，展开后每材料一行 checkbox + 数量。
2. **渲染**：引入 `vendor/supercluster.min.js`（从 BOTWmap 拷）。
   - 每勾选一种材料才建一个 supercluster 实例（懒建缓存，不预建全量）
   - 聚合点 = 材料图标 + 数量数字；点击聚合点自动放大一级；最大 zoom 散成单点
   - 用 Leaflet `L.layerGroup` + 自定义 canvas renderer 或 `L.divIcon` 画
3. **搜索**：材料 Tab 顶部搜索框输入材料名 → 下拉匹配 → 点击勾选该材料 + flyTo 密集区 + 侧栏展开对应类。
4. **详情卡**：点单点 → 现有 #detail 弹窗显示 中文名/分类/坐标/「标记完成」。
5. **三层联动**：切 地上/地下/天空 按钮时，材料图层只显示对应 layer 的点（与现有 markers 分层同机制）。
6. **持久化**：勾选状态存 `localStorage`，键 `totkmap_mats_v1`。

### P2 图标

- 首选：从 `UI/LayoutArchive/Common.Product.110.*.blarc.zs` 解出物品图标 bntx，裁 48px WebP（BOTW 同款流程）。
- 备选（快）：先用统一的绿色圆点/类色图标上线，图标后补；或抓 objmap-totk 网站的物品图标。

---

## 四、工作量预估

| 阶段 | 内容 | 预估 |
|---|---|---|
| P0 | bcett 全量扫点 + 生成 materials.js | 半天（脚本已跑通，补映射表+MSBT） |
| P0.5 | Item.msbt 提取中文名 | 2 小时 |
| P1 | 侧栏 Tab + 手风琴 + supercluster 渲染 + 搜索 | 1-2 天 |
| P2 | 图标提取/裁剪 | 半天 |
| 验收 | PC+手机双端缩放流畅 + 坐标抽样核对 | 半天 |

---

## 五、已拍板决策（2026-09-25 用户确认）

1. **范围**：6 大类（植物/蘑菇/水果/昆虫/鱼/矿岩）93 种 / 21,274 点；**动物狩猎类本期不做**。
2. **地底**：MinusField 5,974 点（主要矿岩）**本期一起上**。
3. **图标**：**一步到位**——先从官方 romfs blarc 里找，找不到再网上找对应图标。
4. **Tab 关系**：材料 Tab 与现有探索 catalogList **并存切换**，Tab 按钮放在搜索栏下方；切 天空/地上/地下 时，材料层与探索层同机制按层过滤（材料点按 Y 自动落层）。
5. **版本号**：V1.6.0 → **V1.7.0**，左下角三处同步。

---

## 六、风险

- TOTK 的 `Item_Fruit_A..P` 泛型 actor 与具体材料名的映射需手工核对（MSBT label 给内部 ID，但图鉴条目名需对照 zeldapedia/wiki 补中文名含义）。
- 地底矿岩 `Obj_Mineral_D_01` 5,423 点占比最大，聚合时需验证 supercluster 在 2 万点量级下手机端帧率。
- 官方物品图标在 blarc 包内，bntx 解图需 SwitchToolbox 或自研 bntx 解码；如耗时过久走备选方案。
