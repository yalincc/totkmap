# TOTKmap · 王国之泪·全能地图（本地离线互动地图）

> 给接手的 AI：本文件是项目唯一交接入口。开工前先读 `AI-CORE` 的
> `RULES.md` + `WORKFLOW.md`（本项目根 `AGENTS.md` 已指路），再通读本文件。
> 磁盘现状为准，别信记忆；`README.md` 只对用户展示功能简介（部署/技术勿写入）。

## 一句话

《塞尔达传说：王国之泪》本地离线互动地图——复刻游民星空互动地图的 UI 与交互，
叠加上下层切换 / 分类筛选 / 搜索 / 存档同步 / 探索度，瓦片用 objmap-totk 的
游戏离线渲染数据（等高线地形风，100% 还原游戏地图观感），纯静态零后端，双击即开。

## 状态

- **V1.4.2 已交付（2026-09-24）**：坐标系根源修复——objmap 瓦片是**游戏内坐标**
  （X∈[-6000,6000] 东向、Z∈[-5000,5000] 北为负），非此前假设的 [-240,0]×[0,288]；
  markers/areas 已按 15 座鸟望塔拟合的仿射变换重写，CRS 改用 objmap 原版 transformation。
  浏览器实测：三层瓦片正常、图标精确落位（监视堡垒等 15 塔全部对齐）、搜索正常、无 console 报错。
- 前身项目（见 AI-CORE PROJECT-INDEX）：
  - `BOTWmap`（旷野之息版，已完成上线）— UI/交互参考源
  - `ZeldaTOTKmap`（读内存实时定位 + 存档进度）— 存档解析方案参考源
- 本目录为**复刻版主项目**，用户 2026-09-24 弃用旧 ZeldaTOTKmap 布局，改以 gamersky
  交互为蓝本重建。

## 目录结构

```
TOTKmap/
├── index.html          产品入口（file:// 双击即开；含 .foot-version/.version 两处版本号）
├── README.md           只写功能简介（勿写部署/技术）
├── css/style.css       全部样式（金色 #eac27e 主调、rgba(13,15,18,.78) 面板）
├── js/
│   ├── app.js          主逻辑（IIFE！末尾 })();）
│   ├── save-parser.js  存档解析（magic 0x01020304 + 哈希表）
│   └── *.bak           app.js / markers.js / areas.js 坐标换算前备份（V1.4.0 留档）
├── data/
│   ├── catalogs.js / layers.js / areas.js / markers.js
│   └── totk_save_hashes.js   CompletismHashes（marcrobledo 提取）
├── vendor/             leaflet / murmurhash3js（本地化，无外网依赖）
├── assets/icons/       59 个分类图标 png
├── tiles/              旧 gamersky 瓦片 z0-5（201.8MB，V1.4.0 后弃用，保留未删）
└── tiles_obj/          新 objmap 瓦片（当前生效）
    ├── ground/  sky/  depths/  各含 z3-z7（z3/z4 由 z5 缩小生成）
    └── 网格：z5=24x20、z6=47x40、z7=94x79（每瓦片 256px）
```

## 版本演进（用户每版验收后交付）

| 版本 | 关键内容 |
|---|---|
| V1.1.0 | UI 1:1 复刻 gamersky + 存档同步（读 progress.sav 哈希表） |
| V1.2.0 | 面板 352px、地名样式、? 帮助弹窗（参考 BOTWmap） |
| V1.3.0-V1.3.2 | 地名米黄→暖白、3px 细滚动条、删区域条、分类名≤6 字无符号、底部探索度+加载存档、标题「王国之泪·全能地图」、图层工具栏、呀哈哈→克洛格 |
| V1.3.3-V1.3.4 | 分类只显总数、探索度金色进度条（无百分比）、龙之泪支持 |
| **V1.4.0** | **objmap 高清瓦片全套替换**（游戏风格）+ 地名暖白深描边 + 探索度进度条统一/分数右对齐 |
| **V1.4.1** | **修复瓦片/图标整体偏移**（TOTK_CRS scale 漏换算）+ 搜索 flyTo infinite tiles 报错 + 自定义标点边界检查 -288→-240 |
| **V1.4.2** | **坐标系根源修复**：objmap 用游戏坐标（非 [-240,0]×[0,288]）；15 塔仿射拟合重写 markers/areas；CRS 换 objmap 原版 transformation；旧自定义标点自动迁移 |

## 经验教训（2026-09-24 坐标系偏移战，全项目最贵一课）

1. **换地图底图 = 换坐标系，先校准再批量换算。** 瓦片源从 gamersky 换 objmap 时，必须先
   确认目标瓦片的**真实坐标系**，而不是按比例猜。V1.4.0 假设 objmap 覆盖 [-240,0]×[0,288]
   并按 `×240/288` 换算 → 图标整体右下偏移；V1.4.1 只补 scale 漏算，**继续在错误前提上打补丁**
   → 偏移依旧。两次都是"假设驱动"，正确做法是"证据驱动"。
2. **逆向源站是最快的真相来源。** 下载 objmap 站点 JS（`app.*.js`），`constructMap` 里
   `transformation=new L.Transformation(4/256,24000/256,4/256,20000/256)` + `toXYZ(e)=[e.lng,0,e.lat]`
   直接暴露坐标系（latlng=(Z,X)、X∈[-6000,6000]、Z∈[-5000,5000]）。别猜，去读源码。
3. **批量坐标换算用锚点拟合仿射变换，不用单点比例。** 15 座鸟望塔（radar API 游戏坐标 ↔
   gamersky 标记坐标）最小二乘拟合，最大误差 <3 游戏单位；单点/比例换算是偏移事故的温床。
4. **坐标换算前先备份原始数据**（`*.bak`），换算公式与脚本存档（`totk_fix_coords.py`），
   发现假设错误时能一键回退重建。
5. **排查"整体偏移"先定位是瓦片错还是标点错**：瓦片与标点同用一套 CRS 时，`scale` 错位会
   让**图标相对瓦片偏移**；坐标系假设错误则**两者都错但彼此一致**（看起来"都对了"反而危险）。
   用锚点地标（塔、神庙）在最大缩放逐点核验，别只看整体观感。

## 技术要点（接手续做必读）

### 地图坐标与瓦片（V1.4.2 核心，最易踩坑）

- **objmap-totk 坐标系（V1.4.2 校正）**：站点源码 `T=24000x20000`（z7 原图像素），
  `CRS.Simple + L.Transformation(4/256, 24000/256, 4/256, 20000/256)`，scale=2^zoom；
  **Leaflet latlng = 游戏坐标 (Z, X)**，X∈[-6000,6000]（东）、Z∈[-5000,5000]（北为负）。
  瓦片网格 z3=6x5 z4=12x10 z5=24x20 z6=47x40 z7=94x79 与 24000x20000 逐级 1/2 吻合。
- **坐标换算（V1.4.2 已重写）**：由 15 座鸟望塔（radar API 游戏坐标 vs gamersky 坐标）最小二乘拟合：
  `lat(Z) = -42.6811380353599 * x_gamersky - 7050.60856084442`
  `lng(X) =  42.6842804369353 * y_gamersky - 6258.74590297329`
  已写入 markers.js(3183) / areas.js(520)（visible 缩放 1/2→3 收敛）；换算前备份 `.bak-v141`。
  V1.4.0/1.4.1 的 `lat×240/288` 是**错误假设**（把 objmap 当 [-240,0]×[0,288]），已废弃。
- `TILE_BOX = L.latLngBounds([-5000,-6000],[5000,6000])`、`CENTER=[-140.80,-298.54]`（监视堡垒）、
  MIN_ZOOM=3、MAX_ZOOM=7、maxNativeZoom=7。
- tileLayer URL：`tiles_obj/{ground|depths|sky}/{z}/{x}_{y}.png`（注意地下是 **depths**，旧代码是 underground）。
- **z3/z4 低层**：objmap 只有 z5-z7，低层由 z5 缩小生成（PIL LANCZOS，4x4/2x2 → 256px）。

### objmap-totk 瓦片源（可续抓，勿重复踩坑）

- 站点：`https://objmap-totk.zeldamods.org/`（zeldamods 系，TotK Object Map）。
- URL 模板：`https://objmap-totk.zeldamods.org/game_files/map/{Ground|Sky|Depths}/maptex/{z}/{x}/{y}.webp`
- **必须带** UA（Chrome 系）+ Referer `https://objmap-totk.zeldamods.org/`，否则 403。
- **网格是实测值，不是 2^n**：z5=24x20、z6=47x40、z7=94x79（坐标从 0 起，无负号）。
  换层/换区域先逐层探测边界（二分探测 x/y 最大存在值），别用公式猜。
- 限流硬约束：并发 >12 必大批失败；用 4-5 线程 + 每请求退避 + 6-7 次重试；
  **一次只跑一个区域/层**（同时跑多个 python 下载任务会莫名 exit -1）。
- z8 及以上不存在（404）。每瓦片 256px webp（转 png 存储）。
- Sky/Depths 网格与 Ground 相同。

### 存档同步（save-parser.js）

- magic `0x01020304`；GAME_VERSIONS：v1.0=2307552 / v1.1.x·v1.2.x=2307656 / v1.4.x=2307856。
- 哈希表偏移 `0x28` 起每 8B `[uint32 hash][uint32 value]`；哨兵 `0xa3db7114`（guidsArray）。
- collect() 键：TOWERS_FOUND / SHRINES_STATUS('Clear') / LIGHTROOTS_STATUS('Open') /
  KOROKS_HIDDEN / KOROKS_CARRY / BUBBULS_GUIDS / SAGE_WILLS_FOUND / ADDISON_COMPLETED /
  SCHEMATICS_STONE/YIGA_FOUND / **龙之泪 = 内联 12 个 DRAGON_TEARS hash（存档无 GUID，实测 0/12）**。
- 用户真实存档：`C:\Users\Administrator\AppData\Roaming\Ryujinx\bis\user\save\0000000000000007\0\slot_00\progress.sav`（Yuzu TitleID `0100F2C0115B6000`）。
- 实测存档进度：神庙 8/152、鸟望台 2/15、树根 0/120、克洛格 4/800、双倍 2/100、
  魔犹伊 3/147、卡邦达 1/81、龙之泪 0/12、贤者遗志 0/20。
- localStorage 键：totkmap_done_v1 / custom_v1 / layer_v1 / names_v1 / save_v1。

### romfs 调研结论（G:\YUZU\Switch Games\TOTKroms）

- **没有现成地图图片**。`TerrainArc\MainField` 是 `.tscb` / `.ta.zs`（3D 地形块/高度图/材质烘焙），
  `WorldMgr` 仅 bgyml 配置。游戏内地图是程序化渲染，转 2D 瓦片需完整地形管线，不现实。
- **地名也非独立文本资源**：objmap 英文地名是画布/贴图渲染，DOM 无文本可抓。
- 结论：**100% 还原游戏地图的唯一现实路径 = objmap-totk 瓦片源**（已落地 V1.4.0）。

## 踩坑清单（接手续做必看）

1. **Edit 工具改 app.js / index.html 必报 "File has not been read yet"** → 一律手写临时
   `.py`（io.open utf-8 newline='\n' 精确 replace + assert）执行后删除。
2. **app.js 是 IIFE**：patch 追加代码必须插到 `})();` 之前，否则 `$ is not defined`。
3. **浏览器缓存**：验证前 URL 加 `?cb=vN&t=时间戳`。
4. **objmap 瓦片裸 urllib 403** → 带 UA+Referer；并发高必限流 → 见上「瓦片源」。
5. **objmap 网格不是标准金字塔**（24x20/47x40/94x79），坐标从 0 起；网页 z2 出现负坐标是显示外扩，不代表真实网格。
6. **坐标换算（V1.4.2 勘误）**：objmap 坐标系 = 游戏坐标 (Z,X)，北为负；gamersky→游戏坐标
   仿射见上文（15 塔拟合）。V1.4.0/1.4.1 的 `lat×240/288` 假设错误，勿再使用；旧换算备份 `.bak-v141`。
7. **低层瓦片**：MIN_ZOOM=3 但 objmap 只有 z5+，必须生成 z3/z4（从 z5 缩小）。
8. **存档端到端测试**：Copy 真实存档为 `_t.sav` → `bu.js(fetch('_t.sav'))` + DataTransfer 构造 File
   派发 change → 测完删除。
9. **移动端模拟**：`Emulation.setDeviceMetricsOverride` 须 navigate 前调用；本轮 CDP session 偶发
   not found（重试/先 resync）。
10. **gamersky API 已死**：裸 POST mapId=18/20 → InvalidRequest，勿再试。
11. **版本号三处同步**：app.js `var VERSION` + index.html `.foot-version` + `.version`（JS 赋值）。
    每次功能改动必须更新（用户硬性要求，左下角可见）。
12. **README 只写功能简介**；部署/技术/开发过程一律不写。

## 待办 / 接手方向

- [x] V1.4.2 用户验收（2026-09-24 已交付并 push 归档；`.bak`/`.bak-v141` 保留在本地未入库，由 git 历史兜底）
- [ ] 移动端视口实测（CDP 环境恢复后跑一遍三层 + 探索度）
- [ ] 存档同步其余类别：神殿、迷你挑战 **存档无记录**（不现实，勿承诺）
- [ ] `tiles/` 旧瓦片 201.8MB 是否清理（问老大，勿擅删；已 gitignore 不入库）
- [ ] 后续功能一律：先方案确认 → 落 `开发计划-<功能>.md` → 动手 → 双端验证 → bump 版本 → 交付
- [ ] 与 gamersky 最大缩放观感差异属瓦片源结构性差异（gamersky z7=130×130 正方形/更精细，
      objmap z7=94×79 真实比例）——可选「瓦片源切换」或抓 gamersky HD z6/z7 备选，先问老大

## 相关项目（AI-CORE PROJECT-INDEX 可查）

- `BOTWmap`（旷野之息版，已完成，UI/交互/存档样式参考）
- `ZeldaTOTKmap`（内存实时定位 + 透明浮窗 + 存档进度解析原始方案）
- 存档解析参考：`marcrobledo/savegame-editors`（zelda-totk.completism.js）、`lud99/totk-unexplored`
